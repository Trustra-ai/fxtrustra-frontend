<!DOCTYPE html>
<html>
<head>
  <title>FxTrustra - Dashboard</title>
  <style>
    body { font-family: Arial; margin: 20px; background: #f9f9f9; }
    table { width: 100%; border-collapse: collapse; margin: 20px 0; }
    th, td { border: 1px solid #ccc; padding: 12px; text-align: left; }
    th { background: #1a3c87; color: white; }
    input { padding: 8px; margin: 5px; }
    button { padding: 10px 20px; background: #1a3c87; color: white; border: none; }
  </style>
</head>
<body>
  <h1>FxTrustra Dashboard</h1>
  <h2>Total Portfolio Value: $<%= totalValue.toFixed(2) %></h2>

  <% if (holdings.length === 0) { %>
    <p>No holdings yet. Add your first crypto below!</p>
  <% } else { %>
    <table>
      <tr><th>Coin</th><th>Amount</th><th>Price (USD)</th><th>Value (USD)</th></tr>
      <% holdings.forEach(h => { %>
        <tr>
          <td><%= h.coinId.toUpperCase() %></td>
          <td><%= h.amount.toFixed(6) %></td>
          <td>$<%= h.price.toFixed(2) %></td>
          <td>$<%= h.value.toFixed(2) %></td>
        </tr>
      <% }) %>
    </table>
  <% } %>

  <h3>Add Holding</h3>
  <form method="POST" action="/add-holding">
    <input type="text" name="coinId" placeholder="e.g. bitcoin" required>
    <input type="number" step="any" name="amount" placeholder="Amount" required>
    <button type="submit">Add</button>
  </form>

  <br><br>
  <a href="/logout">Logout</a>
  <% if (req.user && req.user.role === 'admin') { %>
    <br><a href="/admin">Admin Panel</a>
  <% } %>
</body>
</html>
